[package]
name = "riopa-conformance"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Bounded typed Rust model for the RIOPA conformance contract"

[dependencies]
serde_json = "1"
sha2 = "0.10"

[lib]
path = "src/lib.rs"
