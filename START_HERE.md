# Start here in Codex

This repository is already a Git worktree with reconstructed artifact history. Read `AGENTS.md` and `CODEX_AUTONOMOUS_IMPLEMENTATION.md`, then execute:

```bash
bash scripts/bootstrap_codex_handoff.sh --apply --clone-missing
```

Continue with the generated `.riopa-local/codex/NEXT_WORK_PACKAGE.md`; do not stop after bootstrap.
